---
name: Verify
on:
  workflow_dispatch:
  push:
    branches:
      - main

concurrency:
  group: gha_workflow.verify.yml
  cancel-in-progress: true

permissions:
  id-token: write
  contents: write

jobs:
  verify:
    uses: JamesWoolfenden/.github/.github/workflows/terraform-verify.yml@main
